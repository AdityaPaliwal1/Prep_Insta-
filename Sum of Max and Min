#include <bits/stdc++.h> 
int sumOfMaxMin(int arr[], int n){
	// Write your code here.
	int mini = INT_MAX;
	int maxi = INT_MIN;
	int sum =0;
	for(int i=0;i<n;i++){
		mini = min(mini,arr[i]);
		maxi = max(maxi,arr[i]); 
	}
	sum = mini+maxi;
	return sum;
}
